/**
 * 채팅 화면이 쓰는 안내 문구.
 *
 * **한 곳에 모은 이유** — 웹과 앱이 같은 자리에 같은 말을 써야 한다. 각자 적으면 한쪽만
 * 고쳐져 갈라진다. 실제로 앱 채팅방에는 「빈 방」 안내가 아예 없어서, 방금 만든 방이
 * 흰 화면으로만 보였다(2026-08-11).
 */

/**
 * 차단한 상대의 채팅방에서 **입력칸 자리에** 그리는 안내(#877).
 *
 * 방과 지난 대화는 그대로 두고 보내는 것만 막으므로, 「지워졌다」가 아니라
 * 「다시 대화할 수 있다」로 되돌릴 수 있음을 알린다.
 */
export const CHAT_BLOCKED_NOTICE = '차단한 사용자입니다. 차단을 해제하면 다시 대화할 수 있어요.'

/** 방은 만들어졌지만 아직 한 마디도 오가지 않았을 때. */
export const CHAT_EMPTY_TITLE = '아직 주고받은 메시지가 없어요'

/**
 * 위 제목 아래 한 줄.
 *
 * ⚠️ **차단한 방에서는 이 줄을 안 그린다.** 보낼 수 없는 곳에서 보내라고 하면 안 된다.
 */
export const CHAT_EMPTY_DESCRIPTION = '첫 메시지를 보내 대화를 시작해보세요'
