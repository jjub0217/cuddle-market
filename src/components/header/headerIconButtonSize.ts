/**
 * 헤더 한 줄에 나란히 놓이는 아이콘 단추의 크기(#1000).
 *
 * 검색(돋보기)·알림(종)·햄버거가 저마다 크기를 정하다가 어긋났다.
 * 검색과 햄버거는 `IconButton` 의 기본값 md 를 쓰고 알림만 lg 를 줘서,
 * 좁은 폭에서 아이콘은 셋 다 24px 인데 **종을 담은 상자만 4px 컸다**(36 대 40).
 *
 * 그래서 크기를 여기 한 곳에서 정하고 헤더의 아이콘 단추가 모두 이것을 쓴다.
 *
 * lg 로 고른 이유: 넓은 폭(1024 이상)에서 이미 종이 lg 였다.
 * 검색·햄버거는 거기서 `lg:hidden` 이거나 아예 안 그려지므로 **넓은 폭은 그대로다.**
 * 덤으로 좁은 폭 손가락 과녁이 36px 에서 40px 로 넓어진다.
 *
 * IconButton 의 상자 크기 = 아이콘 + 안쪽 여백 × 2
 *   sm `p-1`(4px) · md `p-1.5`(6px) · lg `p-2`(8px)
 */
export const HEADER_ICON_BUTTON_SIZE = 'lg' as const
