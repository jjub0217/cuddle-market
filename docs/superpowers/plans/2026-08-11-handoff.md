# 이어받기 문서 — 21바퀴 다음

> 2026-08-11(화) 밤에 쓴다. **오늘 연 이슈 열여섯 중 열다섯을 닫았다. 남은 건 #904 하나다.**

## 🔴 다음 세션이 바로 할 일

```
1. 배포된 웹·앱에서 오늘 것 훑기 (10분)   ← 시험이 못 덮는 자리 넷
2. #904 사진 확대창 — 상품 상세부터
   ⚠️ 시작 전에 ㉮/㉯ 를 사용자에게 못 박을 것
```

브랜치는 없다. `main` = `origin/develop` = `32e95806`, 작업 트리 깨끗.

## 1단계 — 눈으로 확인 (배포는 이미 다 끝났다)

오늘 만든 것 중 **아직 못 본 것 넷**이다. 넷 다 타입체크·린트·시험이 못 덮는 자리라 눈으로만 갈린다.

```
① 웹 입력칸 세로 정렬 (#890)
   jsdom 이 글을 배치하지 않아 시험으로 못 덮는다
   → 한 줄일 때 글자가 ＋단추와 나란한가 · Shift+Enter 로 여섯 줄까지 자라는가

② 채팅 목록 상품칸 테두리 (#901)
   outline-variant/40 이 옛 #ebe5e0 보다 한 끗 진하다. 거슬리면 /30 으로 낮춘다

③ 좁은 화면에서 대화를 스크롤할 때
   상품 줄이 위에 붙어 있는가 · 그 아래로 대화가 지나갈 때 겹쳐 보이지 않는가
   (선을 걷어낸 뒤라, 겹치면 「스크롤될 때만 생기는 옅은 그림자」로 처리한다)
   근거: src/features/chatting-page/ChattingPage.tsx 의 sticky top-0 — border-b 를 뺐다

④ 방이 5~6개인 계정으로 채팅 목록
   방 테두리를 걷어내서 어디서 방이 끊기는지 흐릿할 수 있다
   흐리면 선을 되살리기보다 방 사이 간격을 벌린다
```

⚠️ **아이폰 HEIC 사진**은 안드로이드로는 못 가린다. 아이폰 사용자가 생기면 그때 확인한다(줄이는 코드가 webp 로 바꿔 주니 통과할 것으로 보지만, 확인 전에는 추정이다).

## 2단계 — #904 사진 확대창

**시작하기 전에 갈림길부터 못 박는다.** 이슈 본문에 적어 뒀다.

```
㉮ 올리는 크기를 키운다(1600px)   진짜 확대가 된다
                                  웹 4곳·앱 1곳 압축값 + Lambda 규칙 + 옛 사진 재처리
㉯ 지금 크기로 화면 가득까지만      오늘 바로 된다. 나중에 ㉮ 로 올려도 화면은 그대로 쓴다
```

**㉯ 를 권하는 이유** — 인프라부터 하면 잘됐는지 볼 화면이 없다. 화면을 먼저 만들면 나중에 크기를 키우든 프록시로 갈아타든 **주소만 바뀐다.**

```
어디부터   상품 상세 → 채팅 사진 → (커뮤니티 본문)
안 넣는 곳  목록 썸네일 ✗  누르면 상세로 가야 한다. 확대창이 그 길을 막는다
           프로필 사진 ✗

앱   mobile/components/product-detail/image-carousel.tsx
     react-native-gesture-handler · reanimated 가 **이미 들어 있다** — 새 의존성 없이 만든다
     (의존성을 바꾸면 EAS 빌드로 다시 확인해야 한다)
웹   src/features/product-detail/components/
     ⚠️ 모바일 웹에서 모달 안 핀치줌은 브라우저 기본 동작이 잘 안 먹는다. 직접 다뤄야 한다

닫기  앱은 뒤로가기로도 닫힌다(화면을 나가면 안 된다) · 웹은 ESC·바깥 누르기
```

## 지금 열려 있는 것

```
#904  사진 확대창          ← 다음 작업
#614  헤더 높이 FOUC       작음 · 웹만. 큰 것 사이에 끼워 넣기 좋다
#868  관리자 추천 장소      큼 · 관리자 화면+백엔드
#849  계정 열거 막기        큼 · 출시 후로 미뤄 둔 것
#803  이용약관             조사·글쓰기
```

## 이슈로 안 만들고 남겨 둔 것 (셋)

```
① 빈 채팅방 안내 제목이 text-[#1c1b1b] (거의 검정)
   이 화면의 다른 글자는 갈색(on-surface)이다
   ChatLog.tsx:172 · PR #903 과 kimi 둘 다 「별도 이슈로 가는 게 맞다」로 정리
   → 이슈로 만들지부터 정할 것 (색 정리와 눈에 보이는 변화를 따로 결정해야 한다)

② 소켓이 끊긴 동안 온 메시지는 다시 붙어도 안 가져온다 (웹·앱 공통)
   #882·#884 에서 재구독만 고쳤고 재조회는 안 넣었다. PR #885 본문에 적어 뒀다

③ 알림을 더블클릭하면 상품 상세로 간다 (클릭이 새 화면으로 통과)
   되돌릴 수 있고 그 자리에 파괴적 단추가 없어 그냥 두기로 함
   ⚠️ 다만 앞으로 「화면을 옮긴 직후 손가락이 닿는 자리」에 확인창 없는 파괴적 단추를 놓지 말 것
```

## 오늘 겪은 함정 (되풀이하지 말 것)

```
소켓 재구독      stompjs 는 다시 붙어도 구독을 안 살려준다. 「아직 안 걸린 것만」 걸면
                 죽은 껍데기를 건너뛴다. onWebSocketClose 를 반드시 단다
                 (onDisconnect 는 줄이 뚝 끊기면 안 온다)
                 → mobile/AGENTS.md 함정표에 적어 뒀다

서버 enum        웹이 SOLD_OUT 을 보내는데 서버엔 그 값이 없어 기능이 한 번도 안 됐다.
                 응답 DTO 만 보지 말고 **보내는 값**도 열어본다
                 → CLAUDE.md 「백엔드 저장소」에 적어 뒀다

템플릿           이슈·PR 템플릿을 기억으로 지어내면 어긋난다. 관련 이슈는 목록 항목
                 (- Close #번호)이라야 GitHub 가 제목을 펼친다
                 → CLAUDE.md 「이슈·PR 템플릿」 절을 새로 만들었다

prettier         웹에 --write 를 돌리면 관계없는 줄까지 다시 접힌다. 손으로 맞춘다
                 → CLAUDE.md 「게이트」 절에 적어 뒀다

색               같은 색상끼리는 밝기만으로 안 갈린다(갈색 위 갈색). 스크린샷 픽셀을 뽑아
                 숫자로 견주면 「비슷해 보인다」로 놓치는 것을 잡는다
```

## 도구·환경

```
테스트 계정   웹 devel.jjub+web798@gmail.com (닉네임 웹798, 사용자 41)
              앱 devel.jjub+gate798@gmail.com (게이트798, 사용자 37)
              웹 두 계정 테스트는 시크릿 창을 쓴다

켜져 있던 것   Metro(8081) · 웹 dev 서버(3000) — 세션 끝에도 켜 두었다
tmux 병렬     판이 갈라지는지 `tmux list-panes` 로 확인하고 시작한다.
              팀원은 구현·게이트까지만, 커밋은 리드가 한다(.git/index.lock 충돌)
```

## 관련

- 학습 로그 — `~/Desktop/weekly-learning-log.md` 의 「2026-08-11 (화)」
- 백엔드 규칙·로그 보는 법 — 저장소 루트 `CLAUDE.md` 「백엔드 저장소」
- 앱 함정 — `mobile/AGENTS.md`
- 어제 것 — `docs/superpowers/plans/2026-08-10-877-handoff.md`
