# Dependencies
node_modules/

# Next.js
.next/
out/

# Production
build/
dist/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Secrets (서버 접속 정보·키 — 절대 커밋 금지)
ssh.md
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude Code
.claude/settings.local.json

# MCP
.mcp.json
.mcp.local.json

# Sync
sync/

# OMC (oh-my-claudecode)
.omc/

# Misc
*.log

*storybook.log
storybook-static

# 개인 작업 노트 (레포 미포함)
docs/blog-drafts/
docs/brainstorm/
docs/session-logs/

# 로컬 툴링
.claude/worktrees/
claude-code-skills/

# 브라우저로 재려고 잠깐 만드는 스크립트 자리 (playwright 등).
# 다 쓰면 지우지만, 잊어도 커밋되지 않게 여기서 뺀다. **세션이 끝나면 비운다.**
# ⚠️ **앞에 `/` 를 붙여 저장소 뿌리의 것만** 무시한다. 그냥 `probe/` 라고 쓰면 경로 어디에
#    있든 잡아서 `scripts/probe/`(다시 쓸 것을 옮겨 두는 자리)까지 무시된다
#    — 2026-08-25 에 실제로 걸렸다.
/probe/
